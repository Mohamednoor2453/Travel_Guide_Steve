<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Sites</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            background-color: #f8f9fa;
        }

        h1 {
            text-align: center;
            color: #333;
        }

        .site-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
        }

        .site-card {
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 20px;
            margin: 10px;
            width: 300px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            background-color: #fff;
        }

        .site-card h3 {
            margin: 0 0 10px;
            color: #007bff;
        }

        .site-card p {
            margin: 5px 0;
            color: #555;
        }

        .button {
            display: inline-block;
            padding: 8px 12px;
            margin: 5px;
            text-decoration: none;
            color: #fff;
            border-radius: 5px;
            background-color: #007bff;
        }

        .button:hover {
            background-color: #0056b3;
        }

        .button.download {
            background-color: #28a745;
        }

        .button.download:hover {
            background-color: #218838;
        }
    </style>
</head>
<body>
    <h1>All Sites</h1>
    <div class="site-container">
        <% sites.forEach(site => { %>
            <div class="site-card">
                <h3><%= site.name %></h3>
                <p><strong>Location:</strong> <%= site.location %></p>
                <p><strong>Food:</strong> <%= site.food ? 'Available' : 'Not Available' %></p>
                <p><strong>Drinks:</strong> <%= site.drinks ? 'Available' : 'Not Available' %></p>
                <a href="/site/<%= site._id %>" class="button">View Details</a>
                <a href="/sitePdfDownload/<%= site._id %>" class="button download">Download PDF</a>
            </div>
        <% }) %>
    </div>
</body>
</html>
